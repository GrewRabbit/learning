# image-recognition-prompt.md（占位文件，Phase 1 创建，Phase 3 填充实际文本）

> 本文件为 ImageRecognizer 识别 Prompt 占位文件。
> Phase 1 仅创建空文件确保 LLMCaller 在 Phase 2 编译通过（readFile 运行时加载，编译期不依赖文件内容）。
> Phase 3 将填充实际识别 Prompt 文本，用于将图片识别为标准化文本（仅识别不解题）。
