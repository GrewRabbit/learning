# 计费测试体系与DB驱动切换（T8 集成/E2E + T9 部署落地）

**日期**：2026-08-20
**类型**：新增
**影响范围**：`tests/integration-tests/db-billing.test.ts`（新增）、`tests/e2e-tests/specs/billing.spec.ts`（新增）、`playwright.config.ts`（注册 billing spec）、`package.json`（`test:e2e:billing`）、`.env.local`（`GESP6_CACHE_DRIVER=fs → db`）、`docs/operations/数据库整合实施进度.md`（T8 完成章节）

## 变更背景

T8 阶段为数据库整合的测试收尾：以集成测试 + E2E 验证计费与降级链路端到端正确（AC-010~019/025/026），与既有单测分层互补；T9 阶段按执行计划 §六 上线顺序完成部署落地——验证迁移幂等、切换 `GESP6_CACHE_DRIVER=db`、验证旧题缓存命中（AC-024）、全量验证与代码审查收尾。

## 变更内容

### 集成测试 `tests/integration-tests/db-billing.test.ts`（新增 384 行）

- **真实库集成测试 9 用例**（直接连接生产库 `gesp6_billing`，用户已确认可行），覆盖 mock 无法验证的真实 PostgreSQL 行为：
  - AC-005 并发 10 同 sub 建档仅 1 条（users 唯一约束生效）
  - AC-019 新用户建档赠额 = GESP6_FREE_QUOTA_INITIAL
  - AC-011/012 真实 settle 全链路：首获计费 1 次（charged=true、余额-1、consume 流水 1 条、solve_records billed=true）/ 再获免费（charged=false、不新增流水）
  - AC-013 用户维度隔离：A 已获取后 B 首获同 hash → B 计费
  - AC-014 同一用户两种解法（不同 contentHash）→ 各计费 1 次、累计 2 次
  - AC-015 并发 10 个同 (user, contentHash) settle → 仅计费 1 次（唯一约束 + 事务生效）
  - AC-017 余额恰好等于价格：扣费成功、余额归 0；后续请求 GESP6_BILLING_INSUFFICIENT_BALANCE
  - AC-018 人工充值：type=recharge 流水含 operator/remark、余额+3
- 隔离策略：唯一前缀 `e2e-billing-it-{uuid}` 用户/hash，afterEach 逆序清理 + 前缀兜底（断言失败也不残留）；settle 前置用 `insertIfAbsentSolution` 插入 solutions 行满足 FK

### E2E 计费反馈 `tests/e2e-tests/specs/billing.spec.ts`（新增 201 行）

- @no-llm 缓存命中计费（AC-011 缓存命中 + AC-025 前端展示 + AC-012 免费返回）：提交已缓存旧题（B2002，缓存命中不调 LLM）→ /result 横幅「本次已计费 + 剩余额度」/ 再次提交「本次免费」
- @llm 新题首次计费（AC-011 缓存未命中）：唯一新题 → 缓存 miss → 真实生成 → 已计费
- @no-llm 用户维度隔离（AC-013）：a0000000 已获取后 a0000003 独立登录首次获取同题 → 计费
- 幂等前置：beforeAll 经 node 端直连 DB 重置两个测试账户（清计费子表 + 重置余额），保证每 run 从已知状态起步（对齐 T8 定义「前置重置余额」）
- 已注册 `playwright.config.ts` chromium-auth 项目 + `package.json` 新增 `test:e2e:billing`

### T6 前端计费反馈补记（原 T6 提交时按用户指示待 T9 统一记录）

- `app/lib/billing/billing-storage.ts`（新增）：BillingFeedback 两形态 + save/load + isInsufficientBalanceError 判定；写失败仅 logClientError 不中断、读无 key/非法 JSON 返回 null 静默降级
- `app/solve/hooks/use-job-polling.ts`：done 分支将 charged/balanceRemaining 写入 sessionStorage（与 SOLUTION 同生命周期）；error 分支额度不足追加写入 insufficient 标记
- `app/result/components/billing-banner.tsx`（新增）：已计费（Coins）/ 免费（BadgeCheck）/ 剩余额度 / null →「额度暂不可用」/ 充值提示；feedback=null 不渲染（不破坏历史解法展示）
- `app/result/page.tsx`：读取计费反馈 + 渲染 BillingBanner（header 后、WarningBanner 前）

### T9 部署落地

- 迁移幂等验证：`npm run db:migrate` 重跑幂等（版本表 1 条不新增，AC-003）
- 驱动切换：`.env.local` 的 `GESP6_CACHE_DRIVER=fs → db`（已备份原文件于 /tmp/opencode/）
- AC-024 验证：db 驱动下提交导入过的旧题（B2002）→ primaryIndexes → solutions 链路直接命中缓存（validated=true、cached=true），不触发 LLM
- 全量验证：type-check 0 错误 / lint 0 警告 / `npm test` 750 通过 / 4 跳过（单测 723 + 集成 27）

## 涉及文件

| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| `tests/integration-tests/db-billing.test.ts` | 新增 | 真实库集成测试 9 用例 |
| `tests/e2e-tests/specs/billing.spec.ts` | 新增 | 计费反馈 E2E（@no-llm/@llm/AC-013） |
| `playwright.config.ts` | 修改 | 注册 billing.spec 到 chromium-auth 项目 |
| `package.json` | 修改 | 新增 `test:e2e:billing` script |
| `app/lib/billing/billing-storage.ts` | 新增（T6 补记） | 计费反馈存储 |
| `app/lib/billing/__tests__/billing-storage.test.ts` | 新增（T6 补记） | 6 用例 |
| `app/solve/hooks/use-job-polling.ts` | 修改（T6 补记） | done/error 分支写计费信息 |
| `app/result/components/billing-banner.tsx` | 新增（T6 补记） | 计费横幅展示 |
| `app/result/page.tsx` | 修改（T6 补记） | 读取并渲染计费反馈 |
| `.env.local` | 修改 | `GESP6_CACHE_DRIVER=fs → db` |
| `docs/operations/数据库整合实施进度.md` | 修改 | T8 完成章节 + 待办提醒更新 |

## 配置 / 环境变量变化

| 变量名 | 变化 | 说明 |
|--------|------|------|
| `GESP6_CACHE_DRIVER` | `fs → db` | 生产库缓存驱动切换（.env.local） |
| `SSO_TEST_USERNAME_2` / `SSO_TEST_PASSWORD_2` | 新增（E2E 可选注入） | 双账户隔离测试第二账户凭据，缺省回退 a0000003/Sin00cean |

（其余变量无变化）

## 验证方式

- [x] 类型检查：`npm run type-check`（0 错误）
- [x] Lint：`npm run lint`（0 警告）
- [x] 单元+集成：`npm test`（750 通过 / 4 跳过）
- [x] 迁移幂等：`npm run db:migrate`（版本表 1 条）
- [x] AC-024 缓存命中：db 驱动下旧题 B2002 直接命中（不触发 LLM）
- [ ] E2E 实跑：`npm run test:e2e:billing`（待 IDP 网络恢复；当前 auth.happyrabbit.top 不可达）

## 后续影响 / 注意事项

- 回退方案：`.env.local` 切回 `GESP6_CACHE_DRIVER=fs` 即回退文件缓存；`data/gesp6/` 与 `FsHtmlCache` 保留
- E2E 完整认证链路待 IDP 恢复后实跑验证（`npm run test:e2e:billing`）；storageState 的 access_token 过期时需先重新登录
- 测试账户 a0000003 需在 IDP 邮件激活后方可登录（AC-013 用例前置）
- 生产库已有 108/22/9 存量数据 + 测试产生的计费记录；后续运维清理可参考 T8 完成章节的记录