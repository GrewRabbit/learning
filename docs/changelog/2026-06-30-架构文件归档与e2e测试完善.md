# 架构文件归档与 E2E 测试完善

**日期**：2026-06-30
**类型**：文档
**影响范围**：docs/architecture/、tests/e2e-tests/

## 变更背景

项目原始架构文件 `arch-gesp6-web-html-v1.0.md`（v1.8 approved）已与当前代码实现存在差异，继续保留在活跃目录会误导后续开发与 AI Agent 调度。同时项目 E2E 测试覆盖不足，仅 3 个 spec 文件覆盖基础场景，缺少平台 URL、图片上传、API 契约、导航等关键流程测试。

## 变更内容

### 架构文件归档

将过时的架构文件移至归档目录，保留历史可追溯：

- `docs/architecture/arch-gesp6-web-html-v1.0.md` → `docs/architecture/archived/arch-gesp6-web-html-v1.0.md`
- 新建 `docs/architecture/archived/` 目录

### E2E 测试套件完善（测试增补，非业务代码变更）

新增 4 个 spec 文件 + 增强 3 个现有 spec + 增强 2 个 POM：

**新增 spec 文件**：
- `tests/e2e-tests/specs/navigation.spec.ts` — 导航测试 @smoke @fast（5 用例）
- `tests/e2e-tests/specs/api-contract.spec.ts` — API 契约测试 @fast（9 用例）
- `tests/e2e-tests/specs/image-upload.spec.ts` — 图片上传 UI 测试 @fast（5 用例）
- `tests/e2e-tests/specs/platform-input.spec.ts` — 平台 URL 输入测试 @fast（6 用例）
- `tests/e2e-tests/specs/solve-platform.spec.ts` — 平台 URL 完整流程 @critical（3 用例）
- `tests/e2e-tests/specs/solve-image.spec.ts` — 图片上传完整流程 @critical（2 用例）

**增强现有 spec**：
- `tests/e2e-tests/specs/smoke.spec.ts` — 新增浏览器导航 + /api/health 验证
- `tests/e2e-tests/specs/validation.spec.ts` — 新增文本超长 + 平台 URL 校验
- `tests/e2e-tests/specs/solve-text.spec.ts` — 新增缓存命中 + iframe 深度验证

**增强 POM**：
- `tests/e2e-tests/pages/solve-page.ts` — 新增平台 URL / 图片上传 / 清除图片定位器
- `tests/e2e-tests/pages/result-page.ts` — 新增 warningBanner / loadingText / backToSolveLink 定位器

### Middleware 限流测试策略

为绕过 `middleware.ts` 的 5 次/分钟/IP 限流，@fast 和 @critical 测试通过注入 `x-forwarded-for` 头使用保留 IP 段（TEST-NET-1/2/3）隔离不同 spec 文件的请求配额。

## 涉及文件

| 文件路径 | 变更类型 | 说明 |
|---------|---------|------|
| `docs/architecture/archived/arch-gesp6-web-html-v1.0.md` | 移动 | 从活跃目录归档 |
| `docs/architecture/archived/` | 新增 | 归档目录 |
| `tests/e2e-tests/specs/navigation.spec.ts` | 新增 | 导航测试 |
| `tests/e2e-tests/specs/api-contract.spec.ts` | 新增 | API 契约测试 |
| `tests/e2e-tests/specs/image-upload.spec.ts` | 新增 | 图片上传 UI 测试 |
| `tests/e2e-tests/specs/platform-input.spec.ts` | 新增 | 平台 URL 输入测试 |
| `tests/e2e-tests/specs/solve-platform.spec.ts` | 新增 | 平台 URL 完整流程 |
| `tests/e2e-tests/specs/solve-image.spec.ts` | 新增 | 图片上传完整流程 |
| `tests/e2e-tests/specs/smoke.spec.ts` | 修改 | 新增 2 个测试用例 |
| `tests/e2e-tests/specs/validation.spec.ts` | 修改 | 新增 4 个测试用例 |
| `tests/e2e-tests/specs/solve-text.spec.ts` | 修改 | 新增 2 个测试用例 + beforeEach |
| `tests/e2e-tests/pages/solve-page.ts` | 修改 | 新增 7 定位器 + clearImage 方法 |
| `tests/e2e-tests/pages/result-page.ts` | 修改 | 新增 4 定位器 + 修正 warningBanner |

## 配置 / 环境变量变化

无

## 验证方式

- [x] 类型检查：`npx tsc --noEmit`（0 错误）
- [x] @smoke + @fast 测试：38 passed (45.8s)
- [x] @critical 测试：8/9 passed（1 个因 LLM 慢响应超时，非代码缺陷，缓存命中后重跑通过）
- [x] 架构文件已移至 `docs/architecture/archived/`

## 后续影响 / 注意事项

1. **架构文件引用**：后续 Agent 调度禁止引用 `docs/architecture/arch-gesp6-web-html-v1.0.md`（已归档），应直接参考现有代码实现
2. **E2E 测试运行**：
   - `npm run test:e2e:smoke` — 快速验证 @smoke 测试
   - `npm run test:e2e:critical` — 完整流程（需 LLM API + g++，约 14 分钟）
   - `npm run test:e2e` — 全部测试
3. **@critical 测试依赖**：需配置 `.env.local` 中的 LLM API key，且 dev server 须运行在 localhost:3000
4. **限流测试策略**：测试通过 `x-forwarded-for` 头绕过限流，生产环境不受影响
5. **架构文档空缺**：归档后 `docs/architecture/` 暂无活跃架构文件，如需新架构设计应基于当前代码重新生成
