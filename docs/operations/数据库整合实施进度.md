# 数据库与业务系统整合 实施进度

> 依据：[数据库整合执行计划](./数据库整合执行计划.md)（§五 12 步）+ [架构 v1.1 §12 实施指导](../architecture/arch-db-integration-v1.0.md)
> 模式：**逐阶段推进**——每个阶段（T1~T9）完成、验收、更新本文件后停止，等待用户确认再启动下一阶段
> 日期：2026-08-18

## 阶段总览

| 阶段 | 对应架构步骤 | 任务 | 状态 | 完成时间 |
|------|------------|------|------|---------|
| T0 | 前置 | 环境准备（DATABASE_URL 写入 .env.local，密码 URL 编码） | ✅ 完成 | 2026-08-18 |
| T1 | Step 1-2 | 依赖 + db/config.ts + schema.ts + 首迁落地真实库 | ✅ 完成（已验收） | 2026-08-18 |
| T2 | Step 3-4 | connection/errors + 四 DAO + 单测 | ✅ 完成（已验收） | 2026-08-18 |
| T3 | Step 5 | billing-service + 单测 | ✅ 完成（已验收） | 2026-08-18 |
| T4 | Step 6+8 | Solution 扩展/orchestrator 填充/job-store/DbHtmlCache + 单测 | ✅ 完成（已验收） | 2026-08-19 |
| T5 | Step 7 | route.ts 建档 + settle 回调 + fail-closed/fail-open + 单测 | ✅ 完成（已验收） | 2026-08-19 |
| T6 | Step 9 | use-job-polling 写 sessionStorage + result 页展示（含额度不足提示、计费信息缺失降级 + 单测） | ✅ 完成（已验收） | 2026-08-19 |
| T7 | Step 10 | 导入脚本 + 实际执行（AC-021/022/023） | ✅ 完成（已验收，含实测） | 2026-08-19 |
| T8 | Step 11 | 集成测试 db-billing + E2E billing.spec | ✅ 完成（已验收） | 2026-08-20 |
| T9 | Step 12 | changelog + 全量验证 + 代码审查收尾 | ✅ 完成（已验收） | 2026-08-20 |

## 已完成阶段详情

### T0 环境准备 ✅

- DBA 建库建账号完成（用户操作）：PostgreSQL 16.3 @ 172.24.90.51:5432，库 `gesp6_billing`，账号 `gesp6_app`，权限验证全通
- `DATABASE_URL` 等 9 个 DB 变量写入 `.env.local`；密码按 schema 定稿 §3 步骤 6 预设口径，`@` 已 URL 编码为 `%40`

### T1 依赖 + Schema + 迁移落地 ✅（已独立验收）

**产出**：
- 依赖：`drizzle-orm ^0.45.2`、`pg ^8.23.0`（运行时）；`drizzle-kit ^0.31.10`、`tsx ^4.23.12`、`@types/pg ^8.15.6`（dev）
- 新增文件：`app/lib/db/config.ts`（惰性校验+缓存）、`app/lib/db/schema.ts`（8 表）、`app/lib/db/migrate.ts`、`drizzle.config.ts`、`app/lib/db/migrations/0000_*.sql`（生成）+ `0000_*.down.sql`（手写）、`app/lib/db/__tests__/config.test.ts`（7 用例）
- 修改：`package.json`（依赖 + `db:generate`/`db:migrate` scripts）、`.env.local.example`（9 变量登记，值留空）

**验收结果**（总调度独立复核）：
- 真实库 8 表全部存在 + `__drizzle_migrations` 1 条记录；CHECK 约束 46 处（含 8 显式：quota 双列/billing 四条/solve 两条）；命名索引 4 个；FK 9 处（CASCADE 恰 2 处）；复合主键 2 处
- `db:migrate` 重复执行幂等（AC-003 ✅）
- `npm run type-check` 0 错误 / `lint` 0 警告 / `test:unit` 563 通过

**计划内偏差（已评估放行）**：
1. **偏差 A**：`drizzle-kit migrate` CLI 在最小权限账号下因 drizzle migrator 的 `CREATE SCHEMA IF NOT EXISTS` 库级权限检查静默失败 → `db:migrate` 改为程序化入口 `tsx app/lib/db/migrate.ts`，内含 `RestrictedSchemaEnsurePool` 适配层（仅将「schema 已存在」的 CREATE SCHEMA 语句短路为 no-op，其余透传；版本表/事务/幂等簿记仍为 drizzle 官方逻辑，AC-003 语义不变）
2. **偏差 B**：架构 §5.5 的 `migrate.ts` 由原计划步骤 3 提前至 T1 落地
3. 依赖版本超架构目标范围（drizzle-orm 0.45 vs ^0.3x、drizzle-kit 0.31 vs ^0.2x）：装最新稳定版，generate/migrate 实测可用（R-07 处置：实施前验证存在性并锁定，符合预期）
4. drizzle-kit 0.31 输出扁平迁移结构（`0000_*.sql` + `meta/`），非 schema 定稿 §5.2 假设的子目录结构；down.sql 同目录同前缀命名

**遗留（低风险，记录不阻塞）**：
- npm audit 4 个 moderate（drizzle-kit devDep 链 esbuild，不进生产运行时）
- `migrate.ts` 与 `drizzle.config.ts` 的版本表位置为两处独立常量（已交叉注释标注须一致）

### T2 连接池 + 错误分类 + 四 DAO ✅（已独立验收）

**产出**（执行者：nextjs-backend-expert；DAO 粒度按 AR1-010 落定 4 文件）：
- `app/lib/db/errors.ts`：isDbUnavailable（连接码+message 匹配）/ isUniqueViolation(23505) / isForeignKeyViolation(23503) / classifyDbError（两域错误码映射，不含连接串）
- `app/lib/db/connection.ts`：getPool() 惰性单例（max/idle 30s/connectionTimeout/statement_timeout 经 pg 原生连接参数 startup packet 下发）+ getDb() Drizzle 惰性单例 + Db/DbTx/DbClient 类型
- `app/lib/db/daos/`：user-dao（getOrCreateUser 单事务幂等建档含 quota 初始化 + deductFreeFirst/getBalance/addRecharge）、solution-dao（读三分支 + upsert* DO UPDATE + insertIfAbsent* DO NOTHING 带返回布尔）、access-dao（insertAccessIfAbsent）、billing-dao（consume/recharge 流水 + solve_records）
- 单测：`__tests__/`（errors 22 + connection 4 + config 7）+ `daos/__tests__/`（user 10 + solution 14 + access-billing 6），共 **63 用例全 mock 零真实 DB**

**验收结果**（总调度独立复核 + 复跑命令）：
- `npm run type-check` 0 错误 / `npm run lint` 0 警告 / `npm run test:unit` **619 通过 / 4 跳过**
- deductFreeFirst 单条 `UPDATE ... FROM (子查询) prev RETURNING 新旧双列` 与架构 §4.3 定稿 SQL 逐行一致（SET CASE/WHERE 守卫/来源判定，AR1-017 单语句无竞态窗口）
- getOrCreateUser 三步事务（建档→查 id→建账户）语义等价 AD-05（语句序因 quota FK 依赖 users.id 调整，同事务内等价）
- upsert\*（onConflictDoUpdate）与 insertIfAbsent\*（onConflictDoNothing+returning）冲突策略分离断言到位（AR1-001）

**计划内偏差（已评估放行）**：
1. 未建独立 `db.ts`：`export const db = drizzle(getPool())` 会在模块求值期触发 getDbConfig()（DATABASE_URL 缺失时 import 即抛错，违反 AD-02 惰性原则）→ 改为 connection.ts 内 `getDb()` 惰性单例，架构 §5.1 本只定义 getPool()
2. poolMin 不传入 pg Pool（pg 8.x 无 min 预热参数，传了不生效）；配置语义保留在 getDbConfig
3. statement_timeout 用 pg 8.23 原生连接参数（已核实经 startup packet 下发，服务端强制）

**交接 T3 注意**：billingService 直接使用 `getDb()/DbTx`（from connection.ts）；`SolutionWithHash` 本地别名在 T4 类型扩展后自然合并。

### T3 计费服务 ✅（已独立验收）

**产出**（执行者：nextjs-backend-expert）：
- `app/lib/billing/billing-service.ts`（约 230 行，单例导出）：
  - `settleSuccessfulSolution(p)`：单事务六步（access 判定 → 已获取免费分支 / 首次 CASE WHEN 扣减 → insufficient 回滚 → getBalance → consume 流水 → billed 记录），事务完全封装在服务内（R-08），DAO 全部 tx 注入
  - `rechargeBalance(p)`：单事务 addRecharge → getBalance → recharge 流水（contentHash=null，AR1-008）；amount 非正整数前置拒绝（不触 DB）
  - 额度不足：私有 `InsufficientBalanceError` 事务内抛出触发回滚（access 插入一并回滚），外层 instanceof 捕获转 `GESP6_BILLING_INSUFFICIENT_BALANCE`（文案含「余额不足，请联系管理员充值」，FR-019）
  - 错误分类：`classifyDbError(error, 'billing')` → 连接类 `GESP6_BILLING_DB_UNAVAILABLE` / 其余 `GESP6_BILLING_DEDUCT_FAILED`；日志仅记 code+errorName（NFR-005）
  - price 读取：每次调用读 `GESP6_SOLUTION_PRICE`（容错回退 1）
- 单测 `app/lib/billing/__tests__/billing-service.test.ts`：**12 用例全 mock**（复用 mock-db 模式 + useTransactionMock 断言回滚语义）

**验收结果**（总调度独立复核 + 复跑命令）：
- `npm run type-check` 0 错误 / `npm run lint` 0 警告 / `npm run test:unit` **631 通过 / 4 跳过**（新增 12 用例）
- 与架构 §4.1 b 流程、§4.3 SQL 语义、§5.1 签名、§5.3 错误码逐条一致；AR1-020（计费仅看 access，cached/validated 透传断言）、AR1-011（balanceAfter=双列和）、AC-011/012/016 服务层语义覆盖

**交接 T4/T5 注意**：T5 route 层直接调 `billingService.settleSuccessfulSolution`，无需感知事务；`InsufficientBalanceError` 为模块私有，route 仅按 ServiceResult.code 分支。

### T4 类型与编排扩展 + DbHtmlCache ✅（已独立验收，2026-08-19）

**产出**（执行者：nextjs-backend-expert；含前次中断会话遗留的 Step 6 未提交实现，本次统一核对补全 Step 8）：
- **Solution 扩展**（FR-029/AC-027）：`types.ts` 增加 `contentHash: string`（必填）+ `sampleFp?: string`；新增 `BILLING_INFO_STORAGE_KEY = 'gesp6:billing-info'`（无消费方，T6 使用；与任务书字面 'gesp6:billing' 差异已评估：语义等价，保持既有值）
- **orchestrator 统一填充**（AD-08/AR1-002/016）：模块级私有 `fillSolutionIdentity(result, contentHash, sampleFp)`，两分支 getOrCompute 返回后统一调用（写入缓存的 Solution 自带身份）；compute 内部 3 个返回点与 fix-loop 5 个构造点同步携带；主 key 命中提前 return 路径 contentHash 透传缓存携带值、sampleFp 显式清 undefined
- **job-store 扩展**（AD-09/FR-022）：JobRecord + `charged`/`balanceRemaining`；`completeJob` 第三参 billing **可选**（route.ts 现有调用零改动可编译，T5 传真实值）
- **DbHtmlCache**（AD-06/FR-014，402 行）：三 LRU 前置层（max=100/ttl=1h）+ solutionDao 读写 + 单飞（AR1-019）+ 读失败降级 miss（AC-010a）+ 写失败不阻断（NFR-007）+ set 主 key 解析（复用 fs-paths.parsePrimaryKey）
  - **Plan B 回写（终审观察 3 处置）**：sample 命中且 content2 命中时 `upsertSolution(当前hash)` + `upsertSampleIndex(当前fp, 当前hash)` + LRU 双回写——确保 T5 settle 写 user_solution_access 的 FK 不断裂
- **HtmlCache 接口 Promise 化**：三读方法改 `Promise<ServiceResult>`（DbHtmlCache 异步查询必需），连锁：DualKeyHtmlCache/FsHtmlCache async 化、orchestrator 主 key 检查加 await、5 个测试文件适配
- 测试：db-html-cache 单测 26 例（全 mock solutionDao）+ 集成 AC-027 三路径 3 例（15→18）+ job-store billing 3 例
- changelog：`docs/changelog/2026-08-19-DbHtmlCache与Solution身份扩展.md`

**验收结果**（总调度独立复核 + 复跑命令，2026-08-19）：
- `npm run type-check` 0 错误 / `npm run lint` 0 警告 / `test:unit` **666 通过 / 4 跳过** / `test:integration` **18 通过**
- Plan B 双 upsert 代码亲验（L242-243）；fillSolutionIdentity 签名与主 key 路径语义亲验；completeJob 可选参数亲验
- 禁改文件（db/、billing-service、route.ts、middleware、env.ts、.env.local）零触碰确认

**遗留（低风险，记录不阻塞）**：
1. Plan B 回写遇 DB 故障时当前 hash 在 DB 无行 → T5 settle FK 插入失败（与 NFR-007 同域故障场景，changelog 已记录，错误分类为 GESP6_BILLING_DEDUCT_FAILED，属可接受边界）
2. orchestrator.ts 495 行接近 500 上限，后续触碰需拆分
3. 实施过程曾出现并行编辑覆盖事故（子代理已恢复并逐行复核，总调度复跑三绿确认）

**交接 T5 注意**：route.ts 直接 `import { billingService }`、`completeJob(jobId, result.data, { charged, balanceRemaining })`；GET done 响应顶层透出两字段（FR-022）。

### T5 route.ts 业务整合 ✅（已独立验收，2026-08-19）

**产出**（执行者：nextjs-backend-expert）：
- `app/api/solve/route.ts`（386→474 行）：
  - POST 建档（FR-027）：`认证 → Zod → resolvePlatform → 建档 → createJob`（与架构 §4.1「认证→建档→Zod」顺序差异：建档置于全部输入校验后避免非法输入触发 DB 建档；「建档失败不建 job 不触 LLM」语义不变，已注释+changelog 说明）
  - 建档错误码边界（FR-027/AC-001）：`GESP6_DB_UNAVAILABLE` → 503（不 createJob 不触 LLM）；其余 → 500；**fail-open 开启**建档失败放行 `userId=null`（AR1-006）
  - GET done 响应顶层透出 `charged`/`balanceRemaining`（FR-022/AR1-007，与 result 平级）
  - `getFreeQuotaInitial()`：GESP6_FREE_QUOTA_INITIAL 读取容错默认 5
- `app/api/solve/settle-callback.ts`（新增 87 行，防 route.ts 超 500 行）：`settleAndCompleteJob`（FR-028 全分支：userId=null 跳过 settle / settle 成功透传 / 额度不足与其他失败 failJob / 计费 DB 故障 fail-closed→failJob 或 fail-open→completeJob+WARN）+ `isBillingDegradeOpen()` 导出复用
- 完成回调 `.then(async ...)` + `.catch` 兜底（settle 异常不悬挂 promise，转 GESP6_INTERNAL_ERROR）
- 前置取消检查保留于 route（AR1-005：settle 前检查，已取消丢弃不计费）
- 测试：route.test.ts 35→51 用例（+16，全 mock）；changelog `2026-08-19-Solve路由建档计费整合.md`

**验收结果**（总调度独立复核 + 复跑命令，2026-08-19）：
- `npm run type-check` 0 错误 / `npm run lint` 0 警告 / `test:unit` **682 通过 / 4 跳过** / `test:integration` **18 通过**
- settle-callback 六分支亲读核验（null 跳过/成功透传/DB 故障双态/余额不足/兜底）；route 建档四分支（成功/fail-open/503/500）亲读核验；GET done 透出字段亲读核验（L404-414）
- route.ts 474 行 ≤500 ✓；禁改文件（db/、billing-service、job-store、orchestrator、middleware、GET 鉴权、DELETE）零触碰确认
- 用例覆盖：AC-001（503+不建 job 不触 LLM）/AC-016/AC-026（fail-closed+fail-open）/AR1-005/AR1-006/FR-022/FR-027/FR-028/settle 入参完整性

**遗留（记录不阻塞）**：一处防御式分支 `if (!authSub)`（TS strict 收窄，理论不可达，已注释）。

**交接 T6 注意**：前端消费点——GET done 响应 `data.charged`/`data.balanceRemaining`；`BILLING_INFO_STORAGE_KEY = 'gesp6:billing-info'`（types.ts）用于 /solve→/result sessionStorage 传递；`balanceRemaining=null` 展示「额度暂不可用」（FR-030）。

### T6 待启动定义（总调度 2026-08-19 补充定稿，启动前依据）

> 本节为对执行计划 Step 9 的细化补充，补齐三处原先缺失的定义（评估结论见会话记录），作为 T6 启动、实施与验收的约束。

**目标**：轮询 done 分支将计费信息写入 sessionStorage，result 页据此展示本次是否计费与剩余额度；fail-open 放行与额度不足场景前端反馈完整、不破坏既有解法展示。

**交付范围**（对照 FR-030 全三点的显式拆解）：

1. **正常路径展示（FR-030 主体）**：`use-job-polling.ts` 轮询 done 分支读取响应顶层 `data.charged`/`data.balanceRemaining`，写入 sessionStorage（key=`BILLING_INFO_STORAGE_KEY`，与 `SOLUTION/PROBLEM_STORAGE_KEY` 同生命周期：/solve 完成 job 时同时写入，/result 读取后不删除）；`app/result/page.tsx` 读取并展示「本次是否计费 + 剩余额度」。
2. **fail-open 放行展示（FR-030）**：`balanceRemaining === null` 时展示「额度暂不可用」替代具体数值（`charged=false` 对应放行场景）。
3. **额度不足展示（FR-030 第三点，本次补充）**：轮询收到 error `GESP6_BILLING_INSUFFICIENT_BALANCE`（failJob 路径，envelope 200，AR1-007）→ 前端展示错误文案（含「联系管理员充值」提示，message 已由 billing-service 携带）；**不破坏已展示的历史解法**——失败/取消路径不得清除 sessionStorage 既有 `SOLUTION_STORAGE_KEY`，用户返回 /result 仍可见历史结果。
4. **计费信息缺失降级（本次补充）**：直接访问 /result、sessionStorage 被清空或旧数据无计费信息时，隐藏计费区块或以中性文案展示，不渲染错误、不阻断解法展示（与现有「未找到解题结果」无数据分支区分：有 solution 无 billingInfo → 仅隐藏计费区块）。

**测试要求（本次补充修正）**：遵循项目既有测试栈决策（node 环境纯函数单测，`image-uploader.test.ts` 明示不引入 jsdom + @testing-library/react，组件渲染由 E2E 覆盖），T6 测试策略为：
- 纯逻辑抽取 + 单测：将计费信息写入 sessionStorage / 缺失降级判定的逻辑抽为纯函数（如 `app/lib/ai/billing-display.ts` 之类，导出 `billingDisplayText(charged, balanceRemaining)` 等），node 环境 vitest 单测覆盖：正常展示 / `balanceRemaining=null`→「额度暂不可用」/ 无计费信息降级。
- 集成校验：`use-job-polling` 与 `result/page.tsx` 的 sessionStorage 读写、UI 渲染由 T8 E2E billing.spec 端到端覆盖（含「不破坏已展示的历史解法」场景）。
- 依赖栈不变：不新增 jsdom / @testing-library/react。

**验证要点**：type-check / lint / test:unit 全绿；禁改文件（route.ts、db/、billing-service、job-store、orchestrator、middleware）零触碰。

**交接 T7 注意**：T6 仅前端展示，不触碰导入链路。

### T6 前端计费反馈 ✅（已独立验收，2026-08-19）

**产出**（执行者：nextjs-frontend-expert）：
- `app/lib/billing/billing-storage.ts`（新增 79 行）：BillingFeedback 两形态类型（done：`{charged, balanceRemaining}` / 额度不足：`{insufficientBalance, message}`）+ saveBillingFeedback/loadBillingFeedback + isInsufficientBalanceError/Feedback 判定；写失败仅 logClientError 不中断，读无 key/非法 JSON 返回 null 静默降级
- `app/solve/hooks/use-job-polling.ts`：响应类型扩展 charged/balanceRemaining；done 分支 router.push 前写入（与 SOLUTION 同生命周期）；error 分支额度不足追加写入 insufficient 标记（跳转/展示逻辑零改动，FR-019）
- `app/result/components/billing-banner.tsx`（新增 70 行纯展示组件）：已计费（Coins）/免费（BadgeCheck）/剩余额度/null→「额度暂不可用」/充值提示（role=alert，--destructive 软背景）；feedback=null 返回 null 不渲染（历史解法保护）
- `app/result/page.tsx`：useEffect 读 loadBillingFeedback（与读 SOLUTION 同模式）+ 渲染 BillingBanner（header 后、WarningBanner 前，独立于解法内容区）
- 测试：billing-storage 6 + use-job-polling 6 + billing-banner 5 = **17 用例**

**验收结果**（总调度独立复核 + 复跑命令，2026-08-19）：
- `npm run type-check` 0 错误 / `npm run lint` 0 警告 / `test:unit` **699 通过 / 4 跳过** / `test:integration` **18 通过**
- FR-030 四点亲读核验（正常/null/额度不足/不破坏历史解法）+ 执行计划细化点「计费信息缺失降级」落地
- 样式合规：语义 token + lucide-react、无内联 SVG/硬编码色；result/page.tsx 97 行 ≤300
- 禁改文件零触碰确认

**说明（记录不阻塞）**：
1. hook 测试采用纯逻辑等价覆盖（项目 vitest 为 node 环境无 jsdom/@testing-library 的既有决策），完整 fetch→跳转链路由 T8 的 billing E2E 覆盖
2. T5 代码文件已由用户会话间提交（`3bdf2fb`），总调度 `7161eb2` 补充提交进度文件；`da59f51` 提交执行计划 Step 9 细化

### T7 一次性数据导入 ✅（已独立验收，2026-08-19，含真实库实测）

**产出**（执行者：nextjs-backend-expert）：
- `scripts/migrate-fs-cache-to-db.ts`（429 行）：loadEnv(.env.local) → getDbConfig 校验 → 三目录扫描（成对校验/首个 `_` 分割/防御解析）→ 按序导入（solutions → primary → sample，FK 依赖）→ 逐行 insertIfAbsent*（DO NOTHING 幂等）→ 汇总报告 + 失败明细 + 退出码；`@/` 别名导入（tsx ^4 实测解析 tsconfig paths）
- `package.json` 新增 `db:import` script
- 单测 `scripts/__tests__/migrate-fs-cache-to-db.test.ts`：24 例（成对校验 9 + primary 分割 5 + sample 3 + main 编排 5 + loadEnv 2），TDD 先行
- changelog：`docs/changelog/2026-08-19-存量缓存导入数据库.md`

**实测结果（AC-021/022/023 全部达成）**：
- **AC-021**：首导 imported=108/22/9、exit=0；总调度独立 count 复核 `{solutions:108, primary:22, sample:9}` 与扫描统计及 spec 基线一致
- **AC-022 幂等**：二跑全 skipped（108/22/9）、imported=0、exit=0、行数不变；总调度三跑复核同样全 skip
- **AC-023**：临时重命名 1 个 content .json → broken=1 进失败清单、其余 skipped、exit=1、行数不变 → **恢复后 full-skip 复原**；源数据 sha256 总校验和前后一致（零变更证明）
- AC-024（切 db 驱动后缓存命中验证）按计划属 T9 部署阶段

**验收结果**：type-check 0 错误 / lint 0 警告 / test:unit **723 通过 / 4 跳过**；importRows 逐行 + BATCH_SIZE=500 分片实现亲读核验

**已知偏差（评估接受，changelog 已记录）**：
1. `created_at` 语义：设计 §2.1 要求保留 meta.createdAt，但 T2 定稿 `insertIfAbsent*` 签名不携带该字段 → 导入行取 schema defaultNow()（导入时刻）；历史时间不保留。无业务依赖（计费/获取时间用各表自身 defaultNow）；**决策：不回改已验收 DAO 签名**，如未来需保留历史时间走新迁移 + DAO 扩展
2. 等价微调：逐行语句替代「每批一事务」（单行原子 + 失败隔离细化到行，AC-023 实测证明等价）；BatchStat 内联；扫描行附加 broken 计数

**交接 T8/T9 注意**：库内已有存量缓存数据，集成/E2E 测试与 T9 切 `GESP6_CACHE_DRIVER=db` 可直接验证缓存命中（AC-024）。

### T8 集成测试 + E2E 计费反馈 ✅（已独立验收，2026-08-20）

**产出**：
- `tests/integration-tests/db-billing.test.ts`（新增 ~380 行）：**真实库集成测试 9 用例**（直接连接生产库 `gesp6_billing`，用户已确认可行），覆盖 mock 无法验证的真实 PostgreSQL 行为——
  - AC-005 并发 10 同 sub 建档仅 1 条（users 唯一约束生效）
  - AC-019 新用户建档赠额 = GESP6_FREE_QUOTA_INITIAL
  - AC-011/012 真实 settle 全链路：首获计费 1 次（charged=true、余额-1、consume 流水 1 条、solve_records billed=true）/ 再获免费（charged=false、不新增流水）
  - AC-013 用户维度隔离：A 已获取后 B 首获同 hash → B 计费
  - AC-014 同一用户两种解法（不同 contentHash）→ 各计费 1 次、累计 2 次（本次补充，初查遗漏）
  - AC-015 并发 10 个同 (user, contentHash) settle → 仅计费 1 次（唯一约束 + 事务生效）
  - AC-017 余额恰好等于价格：扣费成功、余额归 0；后续请求 GESP6_BILLING_INSUFFICIENT_BALANCE（本次补充，初查遗漏）
  - AC-018 人工充值：type=recharge 流水含 operator/remark、余额+3
  - 隔离策略：唯一前缀 `e2e-billing-it-{uuid}` 用户/hash，afterEach 逆序清理（子表→父表），不污染生产数据；settle 前置用 `insertIfAbsentSolution` 插入 solutions 行满足 FK（schema §4.6/4.8）
- `tests/e2e-tests/specs/billing.spec.ts`（新增 201 行）：计费反馈 E2E——
  - @no-llm：缓存命中旧题（B2002，fs 缓存就绪）→ 轮询 done 含 charged=true + balanceRemaining（AC-025）+ /result BillingBanner 展示；同题再提 → charged=false（AC-012）
  - @llm：唯一新题 → 缓存 miss → 真实生成 → charged=true（AC-011 未命中）
  - AC-013 双账户隔离：a0000000（storageState）+ a0000003（独立登录），IDP 不可用自动 skip
  - 已注册 `playwright.config.ts` chromium-auth 项目（依赖预登录 storageState）+ `package.json` 新增 `test:e2e:billing`

**验收结果**：
- `npm run type-check` 0 错误 / `npm run lint` 0 警告
- `npm test` **750 通过 / 4 跳过**（单测 723 + 集成 27，集成由 18 增至 27，新增 9 真实库用例）
- 真实库独立复核：AC-015 并发后 consume 流水仅 1 条、余额仅扣 1 次（直接查询断言）

**说明（记录不阻塞）**：
1. E2E 完整认证链路**未实跑**：auth.setup 需真实 IDP（auth.happyrabbit.top）登录，当前网络环境该域名超时不可达（`curl` 10s 超时）；storageState 的 access_token 已过期（8/18）且 middleware 本地验签不自动 refresh。此为**环境限制非代码问题**，IDP 恢复后 `npm run test:e2e:billing` 即可实跑。E2E 代码经 type-check/lint 静态校验通过。
2. 集成测试按 T8 定义「真实库用生产库」执行；测试数据用唯一前缀 + 逆序清理，不残留（已验证清理后生产数据无新增）。

**交接 T9 注意**：T8 仅测试，未切 `GESP6_CACHE_DRIVER=db`（AC-024 属 T9 部署验证）；T9 全量验证时留意 code-validator g++ 偶发 flaky。

### T8 待启动定义（总调度 2026-08-20 补充定稿，启动前依据）

> 本节为对执行计划 Step 11 的细化补充，补齐五处原先缺失的定义（评估结论见会话记录），作为 T8 启动、实施与验收的约束。

**目标**：以集成测试 + E2E 验证计费与降级链路端到端正确（AC-010~019/025/026），与既有单测分层互补、不重复不遗漏。

**覆盖边界（与既有测试的分工）**：

| AC | 归属 | 说明 |
|----|------|------|
| AC-021~023 | T7 已完成 | 导入脚本实测，T8 不重复 |
| AC-028 / AC-030 | 代码评审 / git 检查 | 非测试用例，T8 只读确认 |
| AC-029 | DAO 层单测 | T2 注入尝试单测已有，T8 不重复 |
| AC-004~007 / AC-027 | T2/T4 已覆盖 | orchestrator / DAO 单测 |
| AC-010~019 / AC-025 / AC-026 | **T8 集成测试** | 本阶段核心交付 |
| AC-024 / AC-025 前端链路 | T9 部署 + T8 E2E | 缓存命中由 E2E 预验证 |

**测试策略（mock 与真实 DB 的分层）**：
- **服务与链路层（可 mock）**：AC-011~014/016~019/025/026 走 `db-billing.test.ts`，DAO 测试替身 mock 注入（与现有 orchestrator.test.ts 扁平风格一致）。AC-010 故障隔离通过 mock 指定 DAO 抛错构造（spec §6.5 已声明，不依赖真实 DB 故障注入）。AC-026 fail-closed/fail-open 在集成测试用 mock DB 层抛错验证（E2E 真实 DB 在跑，无法模拟 DB 不可用）。
- **并发唯一性（必须真实 DB）**：AC-015 并发 10 请求仅计费 1 次、AC-005 并发建档仅 1 条——唯一约束/行锁/条件 UPDATE 是 PostgreSQL 真实行为，**禁止全 mock 掩盖**。这两条走真实库集成测试（**直接使用生产库 `gesp6_billing`，用户已确认可行**），断言「仅 1 条 billingRecords / users 记录」。
- **E2E 分层（@no-llm / @llm）**：`billing.spec.ts` 中缓存命中路径（AC-011 缓存命中、AC-012 免费返回）**不需要模型** → 标 @no-llm（快、稳、进 `test:e2e:no-llm`）；仅缓存未命中需真实生成 → 标 @llm。避免全 spec 依赖 LLM 导致限流/flaky（playwright.config 已 `workers=1` + 串行）。

**登录与数据隔离（E2E 稳定复现的关键）**：
- **测试账户（两个，用户已确认）**：`a0000000`（既有）+ `a0000003`（新增，密码 `Sin00cean`，需先在 IDP 邮件激活）。两者配合实现 AC-013 用户维度隔离：E2E 中用户 A（a0000000）已获取某 contentHash 后，用户 B（a0000003）首次获取同一解法 → B 计费。
- **余额控制**：单账户反复跑会累积余额，无法稳定复现 AC-016/017 余额耗尽。T8 在 E2E 前置中重置被测账户余额（跑前清 `billingRecords` + 重置 `quota_accounts` 余额，用专门 hash 隔离耗尽场景），保证余额从已知值起步；两个测试账户轮换避免交叉污染。
- **残留记录**：E2E 产生的 billingRecords/solveRecords 用专门测试内容 hash（如 `t8-e2e-*` 前缀解题）隔离标识，便于识别与清理；集成测试真实库用例执行后保留记录（生产库，用户已确认不清理）。

**验收命令**：
- `npm run type-check` / `npm run lint` 全绿
- `npm run test:integration`（新增 db-billing 后全量集成用例通过）
- `npm run test:e2e:no-llm`（billing @no-llm 用例随行）+ `npm run test:e2e:solve` 或针对性 billing @llm 用例
- 真实库复核：AC-015 并发后 billingRecords 仅 1 条（直接查询断言）

**验证要点**：AC-010~019/025/026 逐条对照；禁改文件（T0-T7 已验收文件）零触碰（测试文件允许新增，不改动已验收业务代码）。

**交接 T9 注意**：T8 仅测试，不切 `GESP6_CACHE_DRIVER=db`（AC-024 属 T9 部署验证）。

### T9 部署落地 + 全量验证 + 收尾 ✅（已独立验收，2026-08-20）

**产出**：
- **代码审查收尾**：T8 代码经 nextjs-code-reviewer 审查发现 E2E 3 严重（S1 /result 断言链路断裂、S2 跨 run 不幂等无余额重置、S3 env 污染）+ 3 重要（I1 AC-013 无标签、I2 断言失败数据泄漏、I3 硬编码凭据）+ 2 次要（M1 余额硬编码、M2 死代码），已全部修复：
  - billing.spec.ts 重写：beforeAll 直连 DB 重置测试账户（幂等）、走 SolvePage UI 提交（真实 sessionStorage 链路）、role="status" 选择器、AC-013 标 @no-llm、env 经 SSO_TEST_USERNAME_2/PASSWORD_2 注入 + delete 恢复
  - db-billing.test.ts：cleanup 增加前缀兜底清理（断言失败不残留）、AC-017 用 SOLUTION_PRICE 解耦、删除死代码 createUserDirect/userSolutionAccess 导入
- **迁移幂等验证**（AC-003）：`npm run db:migrate` 重跑幂等（版本表 1 条不新增）
- **驱动切换**：`.env.local` 的 `GESP6_CACHE_DRIVER=fs → db`（原文件已备份 /tmp/opencode/；.env.local 被 gitignore，不入版本库）
- **AC-024 缓存命中验证**：db 驱动下旧题 B2002 → DbHtmlCache（LRU 前置层 → primaryIndexes → solutions）完整服务层链路直接命中（validated=true、cached=true、html 16702 字节），不触发 LLM。验证方法：`tsx --env-file=.env.local` 预加载环境变量后调用导出的 `htmlCache` 单例（断言 `constructor.name === 'DbHtmlCache'` 防时序误建），与 orchestrator 实际使用的实例一致
- **changelog**：`docs/changelog/2026-08-20-计费测试体系与DB驱动切换.md`（含 T6 前端计费反馈补记 + T8 测试 + T9 部署）

**验收结果**：
- `npm run type-check` 0 错误 / `npm run lint` 0 警告
- `npm test` **750 通过 / 4 跳过**（单测 723 + 集成 27）
- 迁移幂等 + AC-024 缓存命中实测通过
- 代码审查修复后全部通过

**说明（记录不阻塞）**：
1. E2E 完整认证链路仍未实跑：auth.happyrabbit.top（IDP）网络不可达，auth.setup 无法登录生成 storageState。此为环境限制；IDP 恢复后 `npm run test:e2e:billing` 即可实跑（billing.spec 已按审查修复，beforeAll 幂等重置已就绪）
2. `.env.local` 已切 db 驱动（生产生效）；回退方案：切回 `GESP6_CACHE_DRIVER=fs`
3. 测试账户 a0000003 需在 IDP 邮件激活后方可用于 AC-013 用例

## 待办提醒（后续阶段注意事项）

- 遗留一：E2E 完整认证链路实跑待 IDP（auth.happyrabbit.top）网络恢复——IDP 恢复后执行 `npm run test:e2e:billing` 验证 AC-011/012/013/025 前端链路（billing.spec 已按代码审查修复并就绪）
- 遗留二：测试账户 a0000003 需在 IDP 邮件激活后方可用于 AC-013 双账户隔离用例
- 遗留三：code-validator g++ 真实编译用例存在偶发 flaky（非 DB 改造相关），全量验证时留意重跑
- 回退方案：`.env.local` 切回 `GESP6_CACHE_DRIVER=fs` 即回退文件缓存；`data/gesp6/` 与 `FsHtmlCache` 代码保留
